export type Pet = {
  id: string;
  name: string;
  age: string;
  image?: string;
  description?: string;
  category?: string;
  breed?: string;
};

export type RootStackParamList = {
  Main: undefined;
};
